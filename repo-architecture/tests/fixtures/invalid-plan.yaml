# intentionally invalid for negative testing:
# - missing evidence on first component
# - layout not in the canonical set
# - only 1 layer populated (min is 3)
title: "Bad Plan"
subtitle: "should fail validate.py"
layout: fancy-grid
style: ocean-teal

layers:
  application:
    - { name: "OnlyOne", subtitle: "no evidence" }
