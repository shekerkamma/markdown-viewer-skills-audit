# Product Roadmap — TicketForge

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 62/62 tasks complete
**Current Phase:** Complete

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** The first phase sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition — a PR appearing from a bug ticket — works as early as possible. Everything else builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. CodeRabbit) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Sandbox safety is non-negotiable.** Every code generation task runs in Docker with `--network=none`. No exceptions, no shortcuts, no "I'll add isolation later."
8. **Pipeline completeness over perfection.** A working end-to-end pipeline with 70% accuracy beats a perfect ticket analyzer with no PR output.

---

## Phase 0: Foundation & Infrastructure

> **Goal:** A running project with FastAPI backend, PostgreSQL database, Docker sandbox capability, and Next.js frontend shell — no features yet, but the full dev environment works.

**Reference sections — read these before starting this phase:**
- PRD: § Technical Architecture, § Data Model (schema only), § Auth Implementation
- Vision: § Vision & Mission (for product context)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-infrastructure`, commit, push, and open a PR for review."

- [x] **TASK-001** — Initialize Python backend project with FastAPI
  Files: `backend/pyproject.toml`, `backend/app/main.py`, `backend/app/config.py`, `backend/Dockerfile`
  Notes: Use `pyproject.toml` with `hatchling` or `setuptools`. Install core deps: `fastapi`, `uvicorn[standard]`, `pydantic`, `pydantic-settings`. Create a minimal FastAPI app with a health check endpoint at `GET /api/health` returning `{"status": "ok"}`. Config via `pydantic-settings` reading from env vars. Verify: `uvicorn app.main:app --reload` starts and `curl localhost:8000/api/health` returns OK.

- [x] **TASK-002** — Set up PostgreSQL with SQLAlchemy async
  Files: `backend/app/db.py`, `backend/alembic.ini`, `backend/alembic/env.py`
  Notes: Install `sqlalchemy[asyncio]`, `asyncpg`, `alembic`. Create async engine and session factory. Configure Alembic for async migrations. Add `DATABASE_URL` to config. Verify: `alembic revision --autogenerate -m "init"` creates a migration file.

- [x] **TASK-003** — Create database schema (all tables)
  Files: `backend/app/models/user.py`, `backend/app/models/team.py`, `backend/app/models/repository.py`, `backend/app/models/ticket.py`, `backend/app/models/pipeline_run.py`, `backend/app/models/event.py`
  Notes: Implement all 6 tables from PRD § Data Model with exact column definitions, constraints, and indexes. Use SQLAlchemy declarative models. Create an Alembic migration and run it. Verify: `alembic upgrade head` creates all tables. Check with `psql` that tables, indexes, and constraints exist.

- [x] **TASK-004** — Set up Docker SDK for sandbox management
  Files: `backend/app/services/docker_service.py`, `sandbox/Dockerfile.sandbox`, `sandbox/entrypoint.sh`
  Notes: Install `docker` Python SDK. Create `DockerService` class with methods: `create_sandbox(repo_url, branch) -> container_id`, `exec_in_sandbox(container_id, command) -> output`, `get_diff(container_id) -> str`, `destroy_sandbox(container_id)`. Sandbox Dockerfile: base on `python:3.11-slim`, install `git`, `gh` CLI, `node` 20, common build tools. Container runs with `--network=none`, `--memory=2g`, `--cpus=1`. Verify: `docker build -f sandbox/Dockerfile.sandbox -t ticketforge-sandbox .` succeeds. Unit test: create container, run `echo hello`, destroy.

- [x] **TASK-005** — Set up Claude API client service
  Files: `backend/app/services/claude_service.py`
  Notes: Install `anthropic` SDK. Create `ClaudeService` class wrapping the Anthropic client. Methods: `analyze(system_prompt, user_prompt, model="claude-sonnet-4-20250514") -> str`, `generate_code(system_prompt, user_prompt, model="claude-opus-4-20250514") -> str`. Include retry logic with `tenacity` (3 retries, exponential backoff). Track token usage per call. Verify: unit test with mock client.

- [x] **TASK-006** — Set up GitHub service (API + gh CLI wrapper)
  Files: `backend/app/services/github_service.py`
  Notes: Install `httpx`. Create `GitHubService` class with methods: `verify_webhook_signature(payload, signature, secret) -> bool`, `get_issue(repo, number, token) -> dict`, `create_pr(repo, branch, title, body, token) -> dict`, `post_comment(repo, issue_number, comment, token)`, `register_webhook(repo, url, secret, token) -> int`. Use `httpx.AsyncClient` for API calls. Verify: unit tests with mocked HTTP responses.

- [x] **TASK-007** — Set up event stream logger service
  Files: `backend/app/services/event_logger.py`
  Notes: Create `EventLogger` class that persists events to the `events` table. Methods: `log(pipeline_run_id, agent_name, event_type, payload)`. Event types: `action`, `observation`, `decision`, `error`. Payload is arbitrary dict stored as JSONB. Verify: integration test — log 3 events, query them back, confirm ordering.

- [x] **TASK-008** — Set up task queue with ARQ
  Files: `backend/app/tasks/__init__.py`, `backend/app/tasks/pipeline.py`, `backend/app/worker.py`
  Notes: Install `arq`. Create worker settings pointing to Redis. Create a placeholder task `run_pipeline(ticket_id)` that logs "Pipeline started for ticket {id}" and returns. Add `REDIS_URL` to config. Verify: start worker with `arq backend.app.worker.WorkerSettings`, enqueue a task, see it execute.

- [x] **TASK-009** — Implement GitHub OAuth flow
  Files: `backend/app/api/auth.py`
  Notes: Implement per PRD § Auth Implementation. `GET /api/auth/github` redirects to GitHub. `GET /api/auth/github/callback` exchanges code for token, creates/updates user, issues JWT. Install `python-jose[cryptography]` for JWT. Add `GITHUB_CLIENT_ID`, `GITHUB_CLIENT_SECRET`, `JWT_SECRET` to config. Verify: manual test — visit `/api/auth/github` in browser, complete OAuth flow, receive JWT.

- [x] **TASK-010** — Create auth middleware and protected route dependency
  Files: `backend/app/api/deps.py`
  Notes: Create FastAPI dependency `get_current_user` that extracts and validates JWT from `Authorization: Bearer` header. Create `require_team_member(team_id)` and `require_team_admin(team_id)` dependencies that check team membership and role. Verify: unit test — valid JWT returns user, expired JWT returns 401.

- [x] **TASK-011** — Initialize Next.js frontend project
  Files: `frontend/package.json`, `frontend/src/app/layout.tsx`, `frontend/src/app/page.tsx`, `frontend/tailwind.config.ts`, `frontend/next.config.js`
  Notes: `npx create-next-app@latest frontend --typescript --tailwind --app --src-dir`. Configure `NEXT_PUBLIC_API_URL` env var. Create a minimal layout with placeholder nav. Login page shows "Sign in with GitHub" button linking to `{API_URL}/api/auth/github`. Verify: `npm run dev` starts, page renders at localhost:3000.

- [x] **TASK-012** — Set up docker-compose for local development
  Files: `docker-compose.yml`, `.env.example`
  Notes: Services: `postgres` (PostgreSQL 16), `redis` (Redis 7), `backend` (FastAPI with hot reload), `frontend` (Next.js dev server). Mount source code as volumes for hot reload. Create `.env.example` with all required env vars documented. Verify: `docker-compose up` starts all services, health check passes.

- [x] **TASK-013** — Add pytest infrastructure and initial tests
  Files: `backend/tests/conftest.py`, `backend/tests/test_api/test_health.py`, `backend/tests/test_services/test_docker_service.py`
  Notes: Install `pytest`, `pytest-asyncio`, `httpx` (for TestClient). Create fixtures for test database, test client, and mock services. Write tests: health check returns 200, Docker service creates/destroys containers (mock Docker SDK). Add `ruff` for linting. Verify: `pytest` passes, `ruff check .` passes.

---

## Phase 1: Pipeline Core — Ticket to Fix

> **Goal:** The end-to-end pipeline works: a GitHub Issue webhook triggers ticket analysis, code generation in a Docker sandbox, and outputs a git diff. No PR creation yet, no review — just ticket → analysis → fix.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-001, FR-002, FR-003, FR-007), § API Specification (Webhooks), § Data Model
- Vision: § Product Strategy > Core User Flows (Flow 1, steps 1-3)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/pipeline-core-ticket-to-fix`, commit, push, and open a PR for review."

- [x] **TASK-014** — Implement GitHub webhook receiver endpoint
  Files: `backend/app/api/webhooks.py`
  Notes: Implement FR-001. `POST /api/webhooks/github`. Validate HMAC-SHA256 signature. Parse `X-GitHub-Event` header. Handle `issues.opened` and `issues.labeled` events. Check label against repo's `trigger_labels`. Create ticket record + enqueue `run_pipeline` task. Idempotent via `(repository_id, github_issue_number)` unique constraint. Respond within 5 seconds. Verify: unit test with sample GitHub webhook payload — ticket created, task enqueued.

- [x] **TASK-015** — Implement base agent class with event stream logging
  Files: `backend/app/agents/base.py`
  Notes: Create `BaseAgent` abstract class. Constructor takes `pipeline_run_id`, `event_logger`. Methods: `log_action(action_type, payload)`, `log_observation(observation)`, `log_decision(decision, confidence)`, `log_error(error)`. Abstract method: `async run(context) -> AgentResult`. `AgentResult` is a dataclass with `success`, `output`, `confidence`, `error`. All agent subclasses inherit this. Verify: unit test — subclass logs events correctly.

- [x] **TASK-016** — Implement content-researcher agent
  Files: `backend/app/agents/content_researcher.py`
  Notes: Implement FR-002. Inherits `BaseAgent`. Uses Claude Sonnet. System prompt: "You are a bug ticket analyst. Extract structured information from the following GitHub Issue." Output JSON: `{ problem_statement, affected_files: [], reproduction_steps: [], severity: "low"|"medium"|"high"|"critical", confidence: 0.0-1.0 }`. If confidence < 0.4, return `success=False` with reason. Log all actions to event stream. Verify: test with 3 sample issue bodies — one well-structured, one minimal, one with only a title.

- [x] **TASK-017** — Implement CodeActAgent for sandbox code generation
  Files: `backend/app/agents/code_act_agent.py`
  Notes: Implement FR-003. Inherits `BaseAgent`. Uses Claude Opus. Flow: (1) receive analysis from content-researcher, (2) create Docker sandbox via `DockerService`, (3) clone repo in sandbox, (4) send Claude a prompt with: analysis, relevant file contents (read via `exec_in_sandbox`), and instruction to generate a fix, (5) apply the fix in sandbox, (6) run existing tests, (7) if tests fail, iterate up to 3 times, (8) extract `git diff` as output. Timeout: 5 minutes. Log all actions. Verify: integration test with a simple test repo containing a known bug — agent generates a fix.

- [x] **TASK-018** — Implement pipeline orchestrator
  Files: `backend/app/tasks/pipeline.py`
  Notes: Wire up the `run_pipeline` ARQ task. Flow: (1) load ticket from DB, (2) create pipeline_run record, (3) run content-researcher, (4) if analysis fails → escalate, (5) run CodeActAgent with analysis, (6) if fix generation fails → escalate, (7) save fix diff to pipeline_run. Update ticket status at each step (`analyzing` → `generating` → done). Verify: end-to-end test — create a ticket record, run pipeline task, confirm pipeline_run has analysis and diff.

- [x] **TASK-019** — Implement escalation handler
  Files: `backend/app/agents/escalation.py`
  Notes: Implement FR-006. When any agent fails or reports low confidence: (1) update ticket status to `escalated`, (2) post a comment on the GitHub Issue via `GitHubService.post_comment()` with structured analysis notes. Comment format: markdown with agent name, failure reason, and any partial analysis. Verify: unit test — escalation posts comment with correct format.

- [x] **TASK-020** — Add webhook endpoint to FastAPI router and test end-to-end
  Files: `backend/app/main.py`, `backend/tests/test_pipeline/test_end_to_end.py`
  Notes: Register webhook router. Write an end-to-end test: send a mock webhook payload → verify ticket created → verify pipeline_run created → verify content-researcher output stored. Use a test repo with a known simple bug. Verify: test passes with mocked external services (Claude API, Docker, GitHub API).

---

## Phase 2: Code Review & PR Creation

> **Goal:** The pipeline is complete end-to-end: ticket → analysis → fix → review → PR. A developer can see a real PR on GitHub created by TicketForge.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-004, FR-005), § API Specification (Webhooks), § Edge Cases & Error Handling (Pipeline Execution, PR Creation)
- Vision: § Product Strategy > Core User Flows (Flow 1, steps 4-5), § Product Strategy > Magic Moment Design

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/code-review-and-pr-creation`, commit, push, and open a PR for review."

- [x] **TASK-021** — Implement code-review-specialist agent
  Files: `backend/app/agents/code_reviewer.py`
  Notes: Implement FR-004. Inherits `BaseAgent`. Uses Claude Sonnet. Reviews the generated diff against 4 dimensions: (1) code style consistency, (2) test coverage — new tests exist for the fix, (3) regression risk — changes don't break existing functionality, (4) security patterns — no hardcoded secrets, no injection vectors. Output: `{ overall: "approve"|"reject"|"escalate", dimensions: { style: {score, notes}, tests: {score, notes}, regression: {score, notes}, security: {score, notes} }, confidence: 0.0-1.0 }`. Reject on critical security issues. Escalate on confidence 0.4–0.6. Approve on confidence > 0.6 with no critical issues. Verify: test with 3 diffs — a clean fix, a fix missing tests, a fix with a hardcoded API key.

- [x] **TASK-022** — Implement PR creator
  Files: `backend/app/agents/pr_creator.py`
  Notes: Implement FR-005. After review passes: (1) create branch `ticketforge/fix-{issue_number}` in sandbox, (2) commit with message `fix: {summary from analysis}`, (3) push branch via `gh` CLI in sandbox (requires GitHub token mounted), (4) create PR via `GitHubService.create_pr()` with body containing: fix summary, files changed, review assessment, and "Fixes #{issue_number}". Update pipeline_run with `pr_number` and `pr_url`. Verify: integration test — PR is created on a test repo with correct title, body, and linked issue.

- [x] **TASK-023** — Wire review and PR creation into pipeline orchestrator
  Files: `backend/app/tasks/pipeline.py`
  Notes: Extend the pipeline: after CodeActAgent succeeds → run code-review-specialist → if approved, run PR creator → update ticket status to `pr_created`. If review rejects → escalate. If PR creation fails → retry once, then escalate with the diff saved. Update pipeline_run duration and status. Verify: end-to-end test — full pipeline from webhook to PR creation.

- [x] **TASK-024** — Handle PR creation edge cases
  Files: `backend/app/agents/pr_creator.py`, `backend/app/services/github_service.py`
  Notes: Handle per PRD § Edge Cases: branch name conflict (append timestamp), push permission denied (escalate), PR creation API error (retry once), target branch updated since clone (attempt rebase, escalate on conflicts). Verify: unit tests for each edge case.

- [x] **TASK-025** — Add pipeline retry capability
  Files: `backend/app/api/dashboard.py`, `backend/app/tasks/pipeline.py`
  Notes: Add `POST /api/v1/teams/:team_id/tickets/:ticket_id/retry` endpoint. Creates a new pipeline_run and re-enqueues the pipeline task. Only allowed for tickets with status `escalated` or `failed`. Verify: API test — retry a failed ticket, new pipeline_run created.

- [x] **TASK-026** — Comprehensive pipeline integration test
  Files: `backend/tests/test_pipeline/test_full_pipeline.py`
  Notes: Write a full integration test covering the happy path: webhook → ticket → analysis → fix → review → PR. Mock external services (Claude, Docker, GitHub API) with realistic responses. Also test: escalation on low-confidence analysis, escalation on review rejection, retry after failure. Verify: all test scenarios pass.

---

## Phase 3: Dashboard — Status & Monitoring

> **Goal:** A functional web dashboard where users can see pipeline status, ticket history, and event logs. The engineering manager persona (Jordan) can monitor what's happening.

**Reference sections — read these before starting this phase:**
- PRD: § UI/UX Requirements (all screens), § Functional Requirements (FR-008, FR-010, FR-011), § API Specification (Auth, Teams, Repositories, Tickets)
- Vision: § User Research > Primary Persona, § User Research > Secondary Personas (Jordan)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/dashboard-status-and-monitoring`, commit, push, and open a PR for review."

- [x] **TASK-027** — Implement team and repository API endpoints
  Files: `backend/app/api/teams.py`, `backend/app/api/repos.py`
  Notes: Implement all team CRUD endpoints from PRD § API Specification > Teams. Implement repo connection endpoints including webhook registration. Apply auth dependencies. Verify: API tests for each endpoint with auth.

- [x] **TASK-028** — Implement ticket and pipeline run API endpoints
  Files: `backend/app/api/tickets.py`
  Notes: Implement ticket list, ticket detail, and event log endpoints from PRD § API Specification > Tickets. Include cursor-based pagination (default 50, max 100). Filter by status and repo. Verify: API tests — list tickets, get detail with pipeline runs, get events for a run.

- [x] **TASK-029** — Build frontend auth flow and protected layout
  Files: `frontend/src/app/page.tsx`, `frontend/src/app/auth/callback/page.tsx`, `frontend/src/app/dashboard/layout.tsx`, `frontend/src/lib/auth.ts`, `frontend/src/lib/api.ts`
  Notes: Login page with "Sign in with GitHub" button. OAuth callback page that receives JWT from query param and stores in cookie/localStorage. Dashboard layout with top nav (team selector, user avatar, settings). Auth guard middleware redirecting unauthenticated users to login. API client with automatic JWT header injection. Verify: complete OAuth flow in browser — login → callback → dashboard shell renders.

- [x] **TASK-030** — Build dashboard main page (pipeline status overview)
  Files: `frontend/src/app/dashboard/page.tsx`, `frontend/src/components/features/StatusCards.tsx`, `frontend/src/components/features/ActivityFeed.tsx`
  Notes: Implement per PRD § UI/UX > Screen: Dashboard. Status summary cards showing ticket counts by status (pending, analyzing, generating, reviewing, pr_created, escalated, failed). Activity feed showing last 50 events. Empty state: "No repositories connected yet." Auto-refresh every 10 seconds. Verify: page renders with mock data, cards link to filtered ticket list.

- [x] **TASK-031** — Build tickets list page
  Files: `frontend/src/app/dashboard/tickets/page.tsx`, `frontend/src/components/features/TicketTable.tsx`
  Notes: Implement per PRD § UI/UX > Screen: Tickets List. Filter bar (status dropdown, repo dropdown). Table with columns: Issue #, Title, Repo, Status, Time, PR link. Cursor-based pagination. Click row → ticket detail. Verify: table renders with API data, filters work, pagination works.

- [x] **TASK-032** — Build ticket detail page with event log
  Files: `frontend/src/app/dashboard/tickets/[id]/page.tsx`, `frontend/src/components/features/EventLog.tsx`, `frontend/src/components/features/PipelineRunCard.tsx`
  Notes: Implement per PRD § UI/UX > Screen: Ticket Detail. Header with issue title + GitHub link. Pipeline runs list with status badges. Event log — chronological list of agent actions with collapsible JSON payload viewer. "Retry" button for escalated/failed tickets. "View PR" link for completed runs. Verify: page shows full pipeline run history and expandable event details.

- [x] **TASK-033** — Build settings page (repository management)
  Files: `frontend/src/app/dashboard/settings/page.tsx`, `frontend/src/components/features/RepoList.tsx`, `frontend/src/components/features/AddRepoDialog.tsx`
  Notes: Implement per PRD § UI/UX > Screen: Settings (Repositories tab). List connected repos with active/inactive toggle and trigger label editor. "Add Repository" button opens dialog listing user's accessible GitHub repos. Selecting a repo triggers webhook registration. Verify: add a repo, toggle active/inactive, edit trigger labels — all persist via API.

- [x] **TASK-034** — Add real-time updates via SSE
  Files: `backend/app/api/sse.py`, `frontend/src/lib/sse.ts`, `frontend/src/components/features/ActivityFeed.tsx`
  Notes: Implement `GET /api/v1/teams/:team_id/events/stream` SSE endpoint. Backend sends new events as they're logged. Frontend `EventSource` client receives events and updates the activity feed and status cards in real-time. Fallback to polling if SSE disconnects. Verify: open dashboard, trigger a pipeline run, see events appear in real-time without page refresh.

---

## Phase 4: Analytics & Billing

> **Goal:** The dashboard shows quality metrics (acceptance rate, MTTR, hours saved) and teams can upgrade to a paid plan via Stripe.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-009, FR-012), § API Specification (Analytics), § Payment Integration, § UI/UX Requirements (Analytics, Settings/Billing)
- Vision: § Product Strategy > Success Metrics

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/analytics-and-billing`, commit, push, and open a PR for review."

- [x] **TASK-035** — Implement analytics API endpoint
  Files: `backend/app/api/analytics.py`
  Notes: Implement FR-009 and PRD § API Specification > Analytics. `GET /api/v1/teams/:team_id/analytics?period=30d`. Calculate: tickets_processed, prs_created, prs_merged, escalations, acceptance_rate, avg_fix_time_seconds, tokens_used, estimated_hours_saved (accepted PRs × 2 hours). Filter by period (7d/30d/90d). Verify: API test with seeded data — metrics calculate correctly.

- [x] **TASK-036** — Build analytics dashboard page
  Files: `frontend/src/app/dashboard/analytics/page.tsx`, `frontend/src/components/features/MetricCard.tsx`, `frontend/src/components/features/TicketChart.tsx`
  Notes: Implement per PRD § UI/UX > Screen: Analytics. Period selector (7d/30d/90d). Metric cards: acceptance rate, MTTR, escalation rate, hours saved, tokens used. Bar chart: tickets over time. Line chart: acceptance rate trend. Use `recharts` for charts. Empty state: "Not enough data yet." Verify: page renders with API data, period selector updates all metrics.

- [x] **TASK-037** — Implement Stripe payment integration (backend)
  Files: `backend/app/api/billing.py`, `backend/app/api/stripe_webhooks.py`
  Notes: Implement per PRD § Payment Integration. `POST /api/v1/teams/:team_id/billing/checkout` creates Stripe Checkout Session. `POST /api/webhooks/stripe` handles events: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`. Install `stripe`. Verify: test with Stripe test mode — checkout session created, webhook updates team plan.

- [x] **TASK-038** — Implement ticket limit enforcement for free tier
  Files: `backend/app/tasks/pipeline.py`, `backend/app/api/billing.py`
  Notes: Before enqueuing a pipeline run, check ticket count for the current month against the team's plan limit (free: 10/month, team: unlimited). If over limit, reject with clear message. Add `GET /api/v1/teams/:team_id/billing/usage` endpoint returning current month usage vs limit. Verify: test — free team at limit gets rejected, team plan processes normally.

- [x] **TASK-039** — Build billing settings tab
  Files: `frontend/src/app/dashboard/settings/page.tsx`, `frontend/src/components/features/BillingTab.tsx`
  Notes: Implement per PRD § UI/UX > Screen: Settings (Billing tab). Show current plan, usage (tickets this month / limit), and upgrade button. Upgrade button redirects to Stripe Checkout. After successful checkout, page refreshes to show "Team" plan active. Verify: complete Stripe test checkout flow in browser.

---

## Phase 5: Polish & Launch Prep

> **Goal:** Production-ready: comprehensive error handling, loading states, rate limiting, landing page, and security hardening. Ready for the first 3 pilot teams.

**Reference sections — read these before starting this phase:**
- PRD: § Non-Functional Requirements, § Edge Cases & Error Handling (all sections), § Security Considerations
- Vision: § Brand Strategy > Voice & Tone Guide, § Product Strategy > Risks

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/polish-and-launch-prep`, commit, push, and open a PR for review."

- [x] **TASK-040** — Add rate limiting to all API endpoints
  Files: `backend/app/main.py`, `backend/app/api/deps.py`
  Notes: Install `slowapi`. Apply rate limits per PRD § Security: 100 req/min on webhook endpoint, 60 req/min per user on API endpoints. Return 429 with `Retry-After` header. Verify: test — exceed rate limit, receive 429.

- [x] **TASK-041** — Implement comprehensive error handling (backend)
  Files: `backend/app/api/error_handlers.py`, `backend/app/main.py`
  Notes: Global exception handler returning consistent error format: `{ error, message, details }`. Handle: validation errors (422), auth errors (401/403), not found (404), rate limit (429), server errors (500). Log all 500s to Sentry. Install `sentry-sdk[fastapi]`. Verify: test each error type returns correct format.

- [x] **TASK-042** — Add loading, empty, and error states to all dashboard pages
  Files: `frontend/src/components/ui/LoadingSkeleton.tsx`, `frontend/src/components/ui/EmptyState.tsx`, `frontend/src/components/ui/ErrorBanner.tsx`
  Notes: Create reusable UI components for loading (skeleton), empty, and error states. Apply to: dashboard main, tickets list, ticket detail, analytics, settings. Use brand voice for messages per PRD § UI/UX states. Error banner includes auto-retry after 5 seconds. Verify: visually check each state by simulating API responses.

- [x] **TASK-043** — Encrypt GitHub access tokens at rest
  Files: `backend/app/services/encryption.py`, `backend/app/models/user.py`
  Notes: Install `cryptography`. Implement AES-256 encryption for `github_access_token` field. Encrypt on write, decrypt on read. Key from `ENCRYPTION_KEY` env var. Migrate existing tokens. Verify: inspect database — tokens are encrypted. API calls using decrypted tokens still work.

- [x] **TASK-044** — Add input validation across all API endpoints
  Files: `backend/app/api/teams.py`, `backend/app/api/repos.py`, `backend/app/api/tickets.py`
  Notes: Ensure all endpoints use Pydantic request models with validation: string lengths, allowed values, required fields. Sanitize user inputs to prevent XSS in stored data. Verify: test with invalid inputs — proper 422 responses with field-level errors.

- [x] **TASK-045** — Build landing page
  Files: `frontend/src/app/page.tsx`, `frontend/src/components/features/LandingHero.tsx`, `frontend/src/components/features/LandingFeatures.tsx`
  Notes: Replace login-only page with a landing page for unauthenticated users. Hero: "Your routine bugs fix themselves." + "Sign in with GitHub" CTA. Features section: 3 value props from Vision § Brand Strategy > Messaging Framework. How it works: 4-step pipeline diagram. Footer with links. Authenticated users redirect to dashboard. Verify: page renders, CTA works, responsive on mobile.

- [x] **TASK-046** — Add SEO and meta tags
  Files: `frontend/src/app/layout.tsx`, `frontend/src/app/page.tsx`
  Notes: Add meta title, description, OG tags, and Twitter card tags. Title: "TicketForge — From ticket to PR. Automatically." Description from the 30-second elevator pitch. Verify: social preview renders correctly (use opengraph.dev or similar).

- [x] **TASK-047** — Performance optimization pass
  Files: various
  Notes: Check against PRD § Non-Functional Requirements > Performance. Dashboard LCP < 2s. API p95 < 200ms. Add database query optimization (check slow queries via EXPLAIN ANALYZE). Add `Cache-Control` headers for static assets. Verify: measure LCP with Lighthouse, measure API latency with simple timing.

- [x] **TASK-048** — Accessibility pass
  Files: various frontend files
  Notes: Check against WCAG 2.1 AA per PRD § Non-Functional Requirements > Accessibility. Ensure: keyboard navigation for all interactive elements, ARIA labels on icon buttons, color contrast ≥ 4.5:1, semantic HTML (headings, landmarks, lists). Verify: tab through entire dashboard, check with axe DevTools extension.

- [x] **TASK-049** — Pipeline resilience improvements
  Files: `backend/app/tasks/pipeline.py`, `backend/app/agents/code_act_agent.py`
  Notes: Handle per PRD § Edge Cases: Claude API down → queue for later. Container timeout → escalate with partial analysis. Repo clone fails → escalate with permission check suggestion. Pre-existing test failures → compare before/after. Add retry with exponential backoff for transient failures. Verify: test each failure scenario.

- [x] **TASK-050** — Add Sentry error tracking to frontend
  Files: `frontend/src/app/layout.tsx`, `frontend/sentry.client.config.ts`
  Notes: Install `@sentry/nextjs`. Configure with DSN. Capture unhandled errors and API failures. Verify: trigger a frontend error, see it in Sentry dashboard.

---

## Phase 6: Post-Launch Iteration

> **Goal:** Enhancements based on pilot feedback. Team management, advanced analytics, and quality-of-life improvements for the 3 pilot teams.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements (FR-009 expanded), § UI/UX Requirements (Settings/Team tab), § Out of Scope (for boundary awareness)
- Vision: § Product Strategy > Feature Priority (Should Have items)

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 6. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-6/post-launch-iteration`, commit, push, and open a PR for review."

- [x] **TASK-051** — Build team management UI
  Files: `frontend/src/components/features/TeamTab.tsx`, `backend/app/api/teams.py`
  Notes: Settings > Team tab: member list with roles, invite via GitHub username, remove member, change role. Backend endpoints: `POST /api/v1/teams/:id/members`, `DELETE /api/v1/teams/:id/members/:user_id`, `PATCH /api/v1/teams/:id/members/:user_id`. Verify: invite a user, change role, remove — all persist.

- [x] **TASK-052** — Add configurable review criteria per repository
  Files: `backend/app/agents/code_reviewer.py`, `frontend/src/components/features/RepoConfig.tsx`
  Notes: Extend repo `config` JSONB to include review criteria overrides: `{ review: { require_tests: true, security_check: true, style_check: true, min_confidence: 0.6 } }`. Code review agent reads these from repo config. UI: checkboxes in repo settings for each criterion. Verify: disable test requirement, review passes without new tests.

- [x] **TASK-053** — Implement fix quality tracking (acceptance/regression)
  Files: `backend/app/services/quality_tracker.py`, `backend/app/api/webhooks.py`
  Notes: Track PR outcomes: listen for `pull_request.closed` webhook events. If merged → mark as accepted. If closed without merge → mark as rejected. Track if any subsequent issues reference the agent PR (potential regression). Store metrics on pipeline_run. Verify: merge a PR → acceptance recorded. Close without merge → rejection recorded.

- [x] **TASK-054** — Add ticket priority queue
  Files: `backend/app/tasks/pipeline.py`, `backend/app/api/tickets.py`
  Notes: Process tickets in priority order based on issue labels: `critical` > `high` > `medium` > `low` > unlabeled. Team plan tickets get priority over free plan. Add `priority` field to tickets table. Verify: enqueue 3 tickets with different priorities, confirm processing order.

- [x] **TASK-055** — Implement Slack notifications for escalations
  Files: `backend/app/services/slack_service.py`, `backend/app/agents/escalation.py`
  Notes: Optional Slack webhook integration. When a ticket is escalated, post a message to the configured Slack channel with: issue link, escalation reason, and analysis notes. Add `slack_webhook_url` to repo config. Verify: escalation triggers Slack message with correct format.

- [x] **TASK-056** — Add CSV/JSON export for analytics
  Files: `backend/app/api/analytics.py`, `frontend/src/app/dashboard/analytics/page.tsx`
  Notes: `GET /api/v1/teams/:id/analytics/export?format=csv&period=30d` returns downloadable CSV with ticket-level data: issue #, status, fix time, acceptance, tokens used. Also support JSON format. Add "Export" button to analytics page. Verify: download CSV, open in spreadsheet, data matches dashboard.

- [x] **TASK-057** — Pipeline performance optimization
  Files: `backend/app/agents/code_act_agent.py`, `backend/app/services/docker_service.py`
  Notes: Optimize: (1) pre-pull sandbox image on startup, (2) shallow clone repos (`--depth 1`), (3) cache dependency installations across runs for same repo, (4) concurrent agent execution where possible (analysis is sequential, but review can run in parallel with PR preparation). Target: reduce average pipeline time from 30 min to 15 min. Verify: benchmark 10 pipeline runs, measure average time.

- [x] **TASK-058** — Add pipeline run cost tracking
  Files: `backend/app/services/cost_tracker.py`, `frontend/src/app/dashboard/analytics/page.tsx`
  Notes: Track per-run costs: Claude API tokens (priced at Sonnet/Opus rates), Docker compute time, GitHub API calls. Show cost per fix on analytics page. Calculate ROI: (hours_saved × hourly_rate) - pipeline_costs. Default hourly rate: $75. Verify: analytics page shows cost breakdown and ROI calculation.

- [x] **TASK-059** — Documentation and README
  Files: `README.md`, `docs/DEPLOYMENT.md`, `docs/CONFIGURATION.md`
  Notes: README: what TicketForge is, quick start (docker-compose up), architecture diagram, link to docs. DEPLOYMENT.md: production deployment guide for VPS + Vercel. CONFIGURATION.md: all env vars documented, repo configuration options, agent tuning parameters. Verify: a new developer can follow README to get running locally.

- [x] **TASK-060** — CI/CD pipeline with GitHub Actions
  Files: `.github/workflows/backend.yml`, `.github/workflows/frontend.yml`
  Notes: Backend: lint (ruff), type check (mypy), test (pytest) on every PR. Frontend: lint (eslint), type check (tsc), build check on every PR. Auto-deploy: backend to VPS on merge to main (via SSH/rsync or Docker push), frontend to Vercel (automatic). Verify: push a PR, CI runs and reports status.

- [x] **TASK-061** — Load testing and scaling validation
  Files: `backend/tests/load/locustfile.py`
  Notes: Install `locust`. Create load test scenarios: (1) 100 concurrent webhook deliveries, (2) 50 concurrent dashboard users, (3) 10 concurrent pipeline runs. Measure: webhook response time, dashboard API latency, pipeline throughput, database query performance. Target: per PRD § Non-Functional > Scalability — 5 teams, 200 tickets/month. Verify: load test results within thresholds.

- [x] **TASK-062** — Production deployment
  Files: `docker-compose.prod.yml`, `scripts/deploy.sh`
  Notes: Production docker-compose with: PostgreSQL (or managed connection string), Redis, backend (Gunicorn + Uvicorn workers), reverse proxy (Caddy or nginx). Deploy script: build images, push to registry, SSH to VPS, pull and restart. Configure SSL via Caddy automatic HTTPS. Set up Sentry DSN, Stripe live keys, production GitHub OAuth app. Verify: deploy to VPS, complete OAuth flow, process a test ticket.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD and vision doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. This catches issues early.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Session Tips

- **Don't read everything:** The PRD and vision doc are large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged. This catches issues early when they're cheap to fix.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/62 tasks complete` and `**Current Phase:** Phase N`.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md, docs/product-vision.md, and docs/design.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md, docs/product-vision.md, and docs/design.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."
